t = -0.005:0.00001:0.005;

x = rectpuls(1000*t,0.002);

fc = 3000; % Cutoff frequency (Hz)
h = 2*fc*sinc(2*fc*t);

y = conv(x, h, 'same');

plot(t,x,'b',t,y,'r');
xlabel('Time (s)');
ylabel('Amplitude');
legend('Input signal x(t)','Output signal y(t)');
